# 正式业务写入路径清单

| 动作 ID | 正式业务动作 | 入口/回调/任务/脚本 | 触发者 | 影响对象 | 写入表/字段/状态 | 必需证据 | 人工确认 | 权限 | 唯一策略源 | 幂等/迟到事件 | 审计 | 回滚 | 正常 Case | 负向/旁路 Case | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ACT-001 | | | | | | | | | | | | | | | 未审计 / 已审计 / HOLD |

## 审计要求

- 全局检查页面、Controller、Service、Mapper、消息消费者、定时任务、AI/异步回调、外部保存、派生产物生成/重建、跨系统同步和运维脚本。
- 重点搜索 `insert`、`update`、`delete`、`create`、`link`、`sync`、`save`、`generate`、`rebuild`、`approve`、`archive`、`setStatus` 和 `setIsTracking`。
- 同一业务动作只能有一个策略源；页面、任务和回调不能各自决定是否放行。
- 未审计路径、互相矛盾的策略源、绕过人工确认的正式状态写入均为 `P0 HOLD`。
- 未获授权时只做静态审计，不执行对象创建、状态变更、派生产物生成、外部同步、结果回写、删除、覆盖或重跑。
